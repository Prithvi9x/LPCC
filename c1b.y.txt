%{
#include <stdio.h>
int yylex(void);
void yyerror(const char *s){ printf("Invalid string\n"); }
%}

%%
S : 'a' S 'a'
  | 'b' S 'b'
  | 'c'
  ;
%%

int main(){
    printf("Enter string:\n");
    if(yyparse()==0) printf("Valid string (center c with symmetry)\n");
    return 0;
}