%{
#include <stdio.h>
int yylex(void);
void yyerror(const char *s){ printf("Invalid string\n"); }
%}

%%
S : 'a' S 'b'
  | 'a' 'b'
  ;
%%

int main(){
    printf("Enter string:\n");
    if(yyparse()==0) printf("Valid string (a^n b^n)\n");
    return 0;
}